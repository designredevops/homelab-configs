# network interface settings; autogenerated
# Please do NOT modify this file directly, unless you know what
# you're doing.
#
# If you want to manage parts of the network configuration manually,
# please utilize the 'source' or 'source-directory' directives to do
# so.
# PVE will preserve these directives, but will NOT read its network
# configuration from sourced files, so do not attempt to move any of
# the PVE managed interfaces into external files!

auto lo
iface lo inet loopback

iface enp0s3 inet manual

auto vmbr0
iface vmbr0 inet static
        address 192.168.1.38/24
        gateway 192.168.1.1
        bridge-ports enp0s3
        bridge-stp off
        bridge-fd 0

auto vmbr1
iface vmbr1 inet static
        address 10.10.10.1/24
        bridge-ports none
        bridge-stp off
        bridge-fd 0

        post-up echo 1 > /proc/sys/net/ipv4/ip_forward
        #post-up   iptables -t nat -A POSTROUTING -s '10.10.10.0/24' -o vmbr0 -j MASQUERADE
        post-up   iptables -t nat -A POSTROUTING -s '10.10.10.0/24' -o vmbr0 -j MASQUERADE && iptables -t raw -I PREROUTING -i fwbr+ -j CT --zone 1
        post-down iptables -t nat -D POSTROUTING -s '10.10.10.0/24' -o vmbr0 -j MASQUERADE

        post-up iptables -t nat -A PREROUTING -i vmbr0 -p tcp --dport 100 -j DNAT --to 10.10.10.100:100
        post-down iptables -t nat -D PREROUTING -i vmbr0 -p tcp --dport 100 -j DNAT --to 10.10.10.100:100
        
        post-up iptables -t nat -A PREROUTING -i vmbr0 -p tcp --dport 8000 -j DNAT --to 10.10.10.101:8000
        post-down iptables -t nat -D PREROUTING -i vmbr0 -p tcp --dport 8000 -j DNAT --to 10.10.10.101:8000
        
        post-up iptables -t nat -A PREROUTING -i vmbr0 -p tcp --dport 9000 -j DNAT --to 10.10.10.101:9000
        post-down iptables -t nat -D PREROUTING -i vmbr0 -p tcp --dport 9000 -j DNAT --to 10.10.10.101:9000

        post-up iptables -t nat -A PREROUTING -i vmbr0 -p tcp --dport 8888 -j DNAT --to 10.10.10.101:8888
        post-down iptables -t nat -D PREROUTING -i vmbr0 -p tcp --dport 8888 -j DNAT --to 10.10.10.101:8888
        
        post-up iptables -t nat -A PREROUTING -i vmbr0 -p tcp --dport 8887 -j DNAT --to 10.10.10.101:8887
        post-down iptables -t nat -D PREROUTING -i vmbr0 -p tcp --dport 8887 -j DNAT --to 10.10.10.101:8887

        post-up iptables -t nat -A PREROUTING -i vmbr0 -p tcp --dport 19999 -j DNAT --to 10.10.10.101:19999
        post-down iptables -t nat -A PREROUTING -i vmbr0 -p tcp --dport 19999 -j DNAT --to 10.10.10.101:19999

        post-up iptables -t nat -A PREROUTING -i vmbr0 -p tcp --dport 8885 -j DNAT --to 10.10.10.101:8885
        post-down iptables -t nat -D PREROUTING -i vmbr0 -p tcp --dport 8885 -j DNAT --to 10.10.10.101:8885
